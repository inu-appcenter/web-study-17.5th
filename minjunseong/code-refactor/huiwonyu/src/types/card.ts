export type LogoInfo = {
  title: string;
  subtitle: string;
};

export type ProfileInfo = {
  position: string;
  name: string;
  address: string;
};

export type Contact = {
  id: string;
  label: string;
  value: string;
  icon?: string;
};
